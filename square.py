#take the input from the user
num=float(input ("Enter a number: "))
#calculate the square root using the exponents
result=num ** 0.5
#display the output
print(f"the square root of {num} is {result}")
